name: Build GKI Kernel with KernelSU Variants v4.4.0 (by deepongi@deepongi-labs)

on:
  workflow_dispatch:
    inputs:
      ksu_variant:
        description: 'KernelSU variant to build'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - tiann
          - kowsu
          - mksu
          - sukisu
          - next
      disable_susfs:
        description: 'Disable SuSFS features'
        required: false
        default: false
        type: boolean
      force_clean_build:
        description: 'Force clean build (ignore cache)'
        required: false
        default: false
        type: boolean
      enable_telegram:
        description: 'Enable Telegram notifications'
        required: false
        default: true
        type: boolean
      create_release:
        description: 'Create GitHub Release with artifacts'
        required: false
        default: true
        type: boolean
      debug:
        description: 'Enable debug output'
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  packages: write

env:
  CLANG_BIN: /mnt/Android/clang-22/bin
  ARM64_TOOLCHAIN: /mnt/Hawai/toolchains/arm-gnu-toolchain-14.3.rel1-x86_64-aarch64-none-linux-gnu/bin/aarch64-none-linux-gnu-
  ARM32_TOOLCHAIN: /mnt/Hawai/toolchains/arm-gnu-toolchain-14.3.rel1-x86_64-arm-none-eabi/bin/arm-none-eabi-
  CCACHE_DIR: /mnt/ccache/.ccache

jobs:
  setup:
    runs-on: [self-hosted, linux, x64]
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      enable_susfs: ${{ steps.config.outputs.enable_susfs }}
      force_clean: ${{ steps.config.outputs.force_clean }}
      build_start_time: ${{ steps.timestamp.outputs.start_time }}
      create_release: ${{ steps.config.outputs.create_release }}
    steps:
      - name: Record Build Start Time
        id: timestamp
        run: |
          echo "WORKFLOW_START_TIME=$(date +%s)" >> $GITHUB_ENV
          START_TIME=$(date +%s)
          echo "start_time=$START_TIME" >> $GITHUB_OUTPUT
          echo "::notice::Workflow started at $(date -u '+%Y-%m-%d %H:%M:%S UTC')"

      - name: Set Build Matrix
        id: set-matrix
        run: |
          VARIANT="${{ github.event.inputs.ksu_variant }}"
          DISABLE_SUSFS="${{ github.event.inputs.disable_susfs }}"
          
          if [ "$VARIANT" = "all" ]; then
            if [ "$DISABLE_SUSFS" = "true" ]; then
              echo 'matrix={"variant":["tiann","kowsu","mksu","sukisu","next"]}' >> $GITHUB_OUTPUT
              echo "🎯 Building all variants (SuSFS disabled)"
            else
              echo 'matrix={"variant":["tiann","kowsu","mksu","sukisu"]}' >> $GITHUB_OUTPUT
              echo "🎯 Building SuSFS variants only (Skipping 'next')"
            fi
          else
            if [ "$VARIANT" = "next" ] && [ "$DISABLE_SUSFS" != "true" ]; then
              echo "❌ ERROR: Cannot build 'next' variant with SuSFS enabled"
              exit 1
            fi
            echo "matrix={\"variant\":[\"$VARIANT\"]}" >> $GITHUB_OUTPUT
            echo "🎯 Building single variant: $VARIANT"
          fi

      - name: Set Build Config
        id: config
        run: |
          echo "enable_susfs=${{ github.event.inputs.disable_susfs == 'true' && 'false' || 'true' }}" >> $GITHUB_OUTPUT
          echo "force_clean=${{ github.event.inputs.force_clean_build }}" >> $GITHUB_OUTPUT
          echo "create_release=${{ github.event.inputs.create_release }}" >> $GITHUB_OUTPUT

  build:
    needs: setup
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
    
    env:
      ENABLE_SUSFS: ${{ needs.setup.outputs.enable_susfs }}
      FORCE_CLEAN: ${{ needs.setup.outputs.force_clean }}
      KSU_VARIANT: ${{ matrix.variant }}

    steps:
      - name: Pre-Clean Workspace (Self-Hosted Guarantee)
        run: |
          echo "🧹 Deep cleaning self-hosted workspace..."
          sudo rm -rf ${{ github.workspace }}/* || true
          sudo rm -rf ${{ github.workspace }}/.* || true

      - name: Checkout Workflow Repo
        uses: actions/checkout@v4
        with:
          clean: false

      - name: Clone Kernel & SuSFS Sources Explicitly
        run: |
          echo "📦 Fetching kernel and SuSFS sources..."
          SUBMODULE_START=$SECONDS
          
          # Clean up any empty placeholder directories 
          rm -rf kernel susfs4ksu
          
          # 1. Clone Google's Android 14 6.1 GKI Kernel
          echo "📥 Downloading Kernel source (android14-6.1-2025-12)..."
          git clone --depth=1 --branch=android14-6.1-2025-12 https://github.com/aosp-mirror/kernel_common kernel
          
          # 2. Clone SuSFS
          echo "📥 Downloading SuSFS source (gki-android14-6.1-dev)..."
          git clone --depth=1 --branch=gki-android14-6.1-dev https://gitlab.com/simonpunk/susfs4ksu susfs4ksu
          
          echo "SUBMODULE_TIME=$((SECONDS - SUBMODULE_START))s" >> $GITHUB_ENV
          
      - name: Clean Kernel Directory Properly
        run: |
          if [ -d "kernel" ]; then
            cd kernel
            git reset --hard HEAD
            git clean -fdx
            rm -rf KernelSU drivers/kernelsu
            rm -f android/abi_gki_protected_exports_* || true
          fi

      - name: Store Commit Info (Workflow, Kernel, SuSFS)
        run: |
          # Workflow
          read -r W_HASH W_SHORT W_DATE W_MSG << EOF
          $(git log -1 --format="%H %h %cd %s" --date=format:'%Y-%m-%d %H:%M:%S')
          EOF
          echo "WORKFLOW_COMMIT_SHORT=$W_SHORT" >> $GITHUB_ENV && echo "WORKFLOW_COMMIT_MSG=$W_MSG" >> $GITHUB_ENV
          
          # Kernel
          cd kernel
          read -r K_HASH K_SHORT K_DATE K_MSG << EOF
          $(git log -1 --format="%H %h %cd %s" --date=format:'%Y-%m-%d %H:%M:%S')
          EOF
          echo "KERNEL_COMMIT_SHORT=$K_SHORT" >> $GITHUB_ENV && echo "KERNEL_COMMIT_MSG=$K_MSG" >> $GITHUB_ENV
          
          # SuSFS
          cd ../susfs4ksu
          read -r S_HASH S_SHORT S_DATE S_MSG << EOF
          $(git log -1 --format="%H %h %cd %s" --date=format:'%Y-%m-%d %H:%M:%S')
          EOF
          echo "SUSFS_COMMIT_SHORT=$S_SHORT" >> $GITHUB_ENV && echo "SUSFS_COMMIT_MSG=$S_MSG" >> $GITHUB_ENV

      - name: Show CCACHE Status
        run: |
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          ccache -s | grep -E "(Cacheable calls|Hits|Misses|Hit rate|cache size)" || true

      - name: Setup KernelSU Variant
        run: |
          KSU_SETUP_START=$SECONDS
          cd kernel && rm -rf KernelSU drivers/kernelsu
          
          case "${{ matrix.variant }}" in
            "tiann") KSU_REPO="tiann/KernelSU"; KSU_BRANCH="main"; USE_SETUP_SH="true" ;;
            "kowsu") KSU_REPO="KOWX712/KernelSU"; KSU_BRANCH="master"; USE_SETUP_SH="true" ;;
            "mksu") KSU_REPO="5ec1cff/KernelSU"; KSU_BRANCH="main"; USE_SETUP_SH="true" ;;
            "sukisu") 
              KSU_REPO="SukiSU-Ultra/SukiSU-Ultra"
              if [ "${{ env.ENABLE_SUSFS }}" = "true" ]; then KSU_BRANCH="tmp-builtin"; USE_SETUP_SH="false"; else KSU_BRANCH="main"; USE_SETUP_SH="true"; fi
              ;;
            "next") KSU_REPO="KernelSU-Next/KernelSU-Next"; KSU_BRANCH="dev"; USE_SETUP_SH="true" ;;
          esac
          
          git clone --depth=1 --branch=$KSU_BRANCH https://github.com/$KSU_REPO.git KernelSU
          
          if [ "$USE_SETUP_SH" = "true" ]; then
            chmod +x KernelSU/kernel/setup.sh && ./KernelSU/kernel/setup.sh .
          else
            cp -r KernelSU/kernel drivers/kernelsu
            grep -q "kernelsu" drivers/Makefile || echo 'obj-$(CONFIG_KSU) += kernelsu/' >> drivers/Makefile
            grep -q "kernelsu" drivers/Kconfig || sed -i '/endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig
          fi
          
          echo "KSU_REPO=$KSU_REPO" >> $GITHUB_ENV && echo "KSU_BRANCH=$KSU_BRANCH" >> $GITHUB_ENV
          echo "KSU_SETUP_TIME=$((SECONDS - KSU_SETUP_START))s" >> $GITHUB_ENV

      - name: Get KSU Version Info & Commit
        run: |
          cd kernel/KernelSU
          git fetch --unshallow --quiet || git fetch --depth=10000 --quiet || true
          
          if [ "${{ matrix.variant }}" = "sukisu" ]; then
            git fetch origin main:main --quiet || true
            KSU_COUNT=$(git rev-list --count main)
            KSU_VERSION=$((40000 + KSU_COUNT - 2815))
          elif [ "${{ matrix.variant }}" = "next" ]; then
            KSU_COUNT=$(git rev-list --count HEAD)
            KSU_VERSION=$((KSU_COUNT + 30000))
          else
            KSU_COUNT=$(git rev-list --count HEAD)
            KSU_VERSION=$((KSU_COUNT + 30000))
          fi
          
          read -r KSU_C_HASH KSU_C_SHORT KSU_C_DATE KSU_C_MSG KSU_C_AUTH << EOF
          $(git log -1 --format="%H %h %cd %s %an" --date=format:'%Y-%m-%d %H:%M:%S')
          EOF
          
          echo "KSU_VERSION=$KSU_VERSION" >> $GITHUB_ENV
          echo "KSU_COMMIT_SHORT=$KSU_C_SHORT" >> $GITHUB_ENV
          echo "KSU_COMMIT_MSG=$KSU_C_MSG" >> $GITHUB_ENV

      - name: Generate Comprehensive Changelog
        if: needs.setup.outputs.create_release == 'true'
        run: |
          mkdir -p changelogs
          CHANGELOG_FILE="changelogs/CHANGELOG-${{ matrix.variant }}.md"
          
          # Optimized: Use heredocs to generate the file cleanly
          cat << EOF > "$CHANGELOG_FILE"
          # 📋 Comprehensive Build Changelog
          
          **Variant:** ${{ matrix.variant }}
          **Build Number:** #${{ github.run_number }}
          **KernelSU Version:** r${{ env.KSU_VERSION }}
          **SuSFS:** ${{ env.ENABLE_SUSFS == 'true' && 'Enabled' || 'Disabled' }}
          
          ---
          ## 📖 Component Versions
          | Component | Current Commit | Date | Summary |
          |-----------|---------------|------|---------|
          | **KernelSU** | \`${{ env.KSU_COMMIT_SHORT }}\` | $(date) | ${{ env.KSU_COMMIT_MSG }} |
          | **Kernel** | \`${{ env.KERNEL_COMMIT_SHORT }}\` | $(date) | ${{ env.KERNEL_COMMIT_MSG }} |
          | **SuSFS4KSU** | \`${{ env.SUSFS_COMMIT_SHORT }}\` | $(date) | ${{ env.SUSFS_COMMIT_MSG }} |
          
          ---
          EOF
          
          echo "CHANGELOG_FILE=$CHANGELOG_FILE" >> $GITHUB_ENV

      - name: Setup SuSFS
        if: env.ENABLE_SUSFS == 'true' && matrix.variant != 'next'
        run: |
          cp susfs4ksu/kernel_patches/fs/susfs.c kernel/fs/
          cp susfs4ksu/kernel_patches/include/linux/susfs.h kernel/include/linux/
          cp susfs4ksu/kernel_patches/include/linux/susfs_def.h kernel/include/linux/
          
          cd kernel
          # Apply core SuSFS patch
          patch -p1 --forward < "../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android14-6.1.patch" || true
          
          # Apply KSU specific SuSFS patch
          if [ "${{ matrix.variant }}" != "sukisu" ]; then
            cd KernelSU
            patch -p1 --forward < "../../susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch" || true
          fi

      - name: Apply Remote Patches & Source Fixes
        run: |
          cd kernel
          
          # 1. CLIDR uninitialized fix
          curl -LSs "https://raw.githubusercontent.com/infectedmushi/kernel_patches/main/fix-clidr-uninitialized.patch" -o /tmp/fix-clidr.patch
          patch -p1 --forward < /tmp/fix-clidr.patch || true
          
          # 2. Fix localversion script appending dirty flags
          sed -i '/# Check for uncommitted changes\./,/fi/d' ./scripts/setlocalversion
          
          # 3. Fix libbpf compilation error (type casting issue with strchr)
          echo "🔧 Patching libbpf.c type casting..."
          sed -i 's/next_path = strchr(s, '"'"':'"'"');/next_path = (char *)strchr(s, '"'"':'"'"');/' tools/lib/bpf/libbpf.c

      - name: Configure Kernel
        run: |
          cd kernel
          export ARCH=arm64
          export PATH="${{ env.CLANG_BIN }}:/usr/lib/ccache:$PATH"
          export CROSS_COMPILE="${{ env.ARM64_TOOLCHAIN }}"
          export CROSS_COMPILE_COMPAT="${{ env.ARM32_TOOLCHAIN }}"
          
          make ARCH=arm64 LLVM=1 LLVM_IAS=1 LD=ld.lld O=out gki_defconfig
          
          # Append all custom GKI, Tensor G3, Net, and KSU configs via cleanly formatted heredoc
          cat << 'EOF' >> out/.config
          CONFIG_ARM64_CORTEX_X3=y
          CONFIG_ARM64_CORTEX_A715=y
          CONFIG_ARM64_CORTEX_A510=y
          CONFIG_ARM64_VA_BITS=48
          CONFIG_ARM64_PA_BITS=48
          CONFIG_SCHED_MC=y
          CONFIG_SCHED_CORE=y
          CONFIG_ENERGY_MODEL=y
          CONFIG_CPU_FREQ=y
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_THERMAL=y
          CONFIG_CMA=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_TCP_CONG_BBR=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_LTO_CLANG_THIN=y
          CONFIG_IP_SET=y
          CONFIG_NET_SCH_CAKE=y
          CONFIG_KSU=y
          CONFIG_LOCALVERSION="-deepongi+"
          CONFIG_KSU_LSM_SECURITY_HOOKS=y
          EOF
          
          if [ "${{ env.ENABLE_SUSFS }}" = "true" ] && [ "${{ matrix.variant }}" != "next" ]; then
            cat << 'EOF' >> out/.config
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          EOF
          fi
          
          make ARCH=arm64 LLVM=1 LLVM_IAS=1 LD=ld.lld O=out olddefconfig

      - name: Build Kernel (Make)
        run: |
          cd kernel
          export ARCH=arm64
          export PATH="${{ env.CLANG_BIN }}:/usr/lib/ccache:$PATH"
          export CROSS_COMPILE="${{ env.ARM64_TOOLCHAIN }}"
          export CROSS_COMPILE_COMPAT="${{ env.ARM32_TOOLCHAIN }}"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CC="ccache clang"
          export CXX="ccache clang++"
          
          MAKE_JOBS=$(nproc)
          START_TIME=$SECONDS
          
          if [ "${{ env.FORCE_CLEAN }}" = "true" ]; then
            make -j$MAKE_JOBS LLVM=1 LLVM_IAS=1 LD=ld.lld O=out clean
          fi
          
          # Crucial Make Command (Exactly as requested)
          make -j$MAKE_JOBS LLVM=1 LLVM_IAS=1 LD=ld.lld HOSTLD=ld.lld O=out CC="$CC"
          
          echo "BUILD_TIME=$((SECONDS - START_TIME))s" >> $GITHUB_ENV

      - name: Package Kernel (AnyKernel3)
        run: |
          cd kernel
          KERNEL_VERSION=$(grep "^VERSION" Makefile | cut -d' ' -f3).$(grep "^PATCHLEVEL" Makefile | cut -d' ' -f3).$(grep "^SUBLEVEL" Makefile | cut -d' ' -f3)
          cd ..
          
          case "${{ matrix.variant }}" in
            "tiann") AK3_BRANCH="KernelSU" ;;
            "kowsu") AK3_BRANCH="KowSU" ;;
            "mksu") AK3_BRANCH="MKSU" ;;
            "sukisu") AK3_BRANCH="sukisu" ;;
            "next") AK3_BRANCH="next" ;;
          esac
          
          git clone --depth=1 --branch=$AK3_BRANCH https://github.com/deepongi-labs/AnyKernel3-p8a AnyKernel3
          
          cp kernel/out/arch/arm64/boot/Image AnyKernel3/
          [ -f kernel/out/arch/arm64/boot/dtb ] && cp kernel/out/arch/arm64/boot/dtb AnyKernel3/
          
          cd AnyKernel3 && rm -rf .git .github
          ZIP_NAME="AK3-${KERNEL_VERSION}-${{ matrix.variant }}-r${{ env.KSU_VERSION }}.zip"
          zip -r9 "../$ZIP_NAME" .
          cd ..
          
          echo "KERNEL_ZIP=$ZIP_NAME" >> $GITHUB_ENV

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kernel-${{ matrix.variant }}-${{ github.run_number }}
          path: |
            ${{ env.KERNEL_ZIP }}
            ${{ env.CHANGELOG_FILE }}

  create_release:
    needs: [setup, build]
    runs-on: [self-hosted, Linux, X64]
    if: needs.setup.outputs.create_release == 'true' && success()
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Create GitHub Release
        run: |
          cd release-assets
          find . -type f -exec mv {} . \; 2>/dev/null || true
          
          RELEASE_NOTES="**Build Date:** $(date)
          **SuSFS:** ${{ github.event.inputs.disable_susfs == 'true' && '❌ Disabled' || '✅ Enabled' }}
          
          ### Devices Supported
          - Pixel 8, 8a, 8 Pro
          
          Flash ZIP in Kernel Flasher or extract Image to patch boot.img manually."
          
          gh release create "build-${{ github.run_number }}" \
            --title "Android 14 Kernel Build #${{ github.run_number }}" \
            --notes "$RELEASE_NOTES" \
            --repo "${{ github.repository }}" \
            ./*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify:
    needs: [setup, create_release, build]
    runs-on: [self-hosted, Linux, X64]
    if: always() && github.event.inputs.enable_telegram == 'true'
    steps:
      - name: Send Telegram Notification
        run: |
          STATUS="${{ needs.build.result }}"
          ICON="✅"
          [ "$STATUS" != "success" ] && ICON="❌"
          
          # Clean heredoc for Telegram Message
          cat << EOF > tg_message.txt
          $ICON *Build #${{ github.run_number }} | ${STATUS^^}*
          ━━━━━━━━━━━━━━━━━━━━━
          📱 *Target:* Pixel 8a/8/8 Pro | GKI Make
          🛡️ *SuSFS:* ${{ github.event.inputs.disable_susfs == 'true' && 'Disabled' || 'Enabled' }}
          
          🔗 [View GitHub Actions Run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
          EOF
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d text="$(cat tg_message.txt)" \
            -d parse_mode="Markdown" \
            -d disable_web_page_preview=true
